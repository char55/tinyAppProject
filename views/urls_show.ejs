<!DOCTYPE html>
<html>
<head>
  <h1><title>The long URL and its shortened form.</title></h1>
</head>
<body>
    <header>
        <% include ./partials/_header %>
    </header>

    <h2>ShortURL: <%= shortURL %></h2>
    <form method="POST" action="/urls/<%=shortURL%>">
        <input type=text name='longURL' value=<%=longURL %>>
        <button type="submit" value=<%=shortURL%>>update</button>
    </form>
    <br>
    <br>
    <br>
    <% if(user) { %>
     <a href="/urls"><button>return to user's URL list</button></a>
   <% } else { %>
     <a href="/login"><button>Would you like to log in?</button></a>
   <% } %>
  </body>
</html>